name: vgv
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.32.0"

dependencies:
  bloc: ^8.1.4
  connectivity_client:
    path: packages/connectivity_client
  connectivity_plus: ^6.1.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  flutter_dotenv: ^6.0.0
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  go_router: ^17.1.0
  intl: ^0.20.2
  local_storage_client:
    path: packages/local_storage_client
  permission_client:
    path: packages/permission_client
  shared_preferences: ^2.3.5
  supabase_auth_client:
    path: packages/supabase_auth_client
  supabase_flutter: ^2.12.0

dev_dependencies:
  bloc_test: ^9.1.7
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mocktail: ^1.0.4
  patrol: ^4.1.0
  patrol_finders: ^3.2.0
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - .env
